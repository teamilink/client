import { Container } from "@mui/material";
import React from "react";

const AppearanceEditor = (props) => {
  return (
    <Container sx={{ display: "flex", flexDirection: "column", width: "50%" }}>
      <h1>AppearanceEditor</h1>
    </Container>
  );
};

export default AppearanceEditor;
